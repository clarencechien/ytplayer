# 字幕可讀性：CPS、行長、行數（R1–R5）

> 狀態：**R1 已實作並實測通過**（2026-08-17，prompt v6）；
> **R4b + R5 已實作**（2026-08-21，`/patch?mode=cps` + `/admin` 📏 壓縮鈕 + 零成本剝夾註）；
> R2–R3、R4a 未實作（**不是評估後否決** —— 決策欄留白等 R1 後的重量，
> 那次重量已經做完，結果與建議在 §3.3）。決策欄在 §8。
> 剩餘工作量約**1.5 天**（R2 一天 + R3 半天；R4a 綁 R2b）。
> 起因：2026-08-17 盤點「斷句跟人做的不一樣」時，量出我們在**尾巴 3%** 的句子上
> 同時違反三條業界規範。
> 先查過有沒有現成 skill 可用（字幕／翻譯／在地化）—— **沒有**，所以照規範自己做。

## 1. 業界規範（Netflix 繁中 Timed Text Style Guide）

| 項目 | 規範 | 為什麼 |
|---|---|---|
| 每行字數 | **≤ 16 全形字** | 視線橫掃的極限 |
| 行數 | **≤ 2 行** | 超過會遮畫面、眼睛要上下找 |
| 閱讀速度 | **≤ 9 字/秒（CPS）** | 中文字資訊密度高 —— 英文是 20 字元/秒 |
| 單塊時長 | 5/6 秒 ～ 7 秒 | 太短來不及讀、太長會以為卡住 |

來源：[Netflix zh-Hant Timed Text Style Guide](https://partnerhelp.netflixstudios.com/hc/en-us/articles/215994807-Chinese-Traditional-Timed-Text-Style-Guide)、
[閱讀速度怎麼算](https://partnerhelp.netflixstudios.com/hc/en-us/articles/115001352212-How-is-reading-speed-measured-Do-punctuation-and-spaces-count)（標點與空白都算）

## 2. 我們現在的狀況（2026-08-17 實測 production 資料）

| | 中文字數中位數 | p90 | 最長 | 顯示秒數中位數 | 最長 |
|---|---|---|---|---|---|
| `JM-oaFXgaSg`（119 句）| 12 | 17 | 29 | 2.8s | 8.3s |
| `kCIvgJklMWQ`（512 句）| 13 | 22 | 44 | 2.5s | 14.0s |

**大部分是健康的**（中位數 12–13 字遠低於 16）。問題全在尾巴：

```
2.0 秒 | 正如其名，這棟房子周邊自然環境非常豐富，視野也極佳，今天就來帶大家參觀。
```

36 字 ÷ 2.0 秒 = **18 CPS（規範上限的兩倍）**，而且 36 字要 3 行才放得下。
`kCIvgJklMWQ` 有 18 句（3%）≥28 字，另有單句掛 14 秒的（規範上限 7 秒）。

**病灶有三個，彼此獨立**：

| 病灶 | 現象 | 修法 |
|---|---|---|
| 譯文太長，時間根本不夠讀 | 18 CPS | **R1 壓縮譯文**（prompt 給字數預算）**✅ 已做** |
| 譯文長度合理，但一行塞不下 | 36 字擠成一行 | **R2 顯示折行／拆塊** |
| 看外語時想回頭看前一句 | 前一句瞬間消失 | **R3 前一句留著**（產品決策）|

## 3. R1 — 壓縮譯文（prompt 給字數預算）· 半天

**這是產業對「時間不夠讀」的真正答案**：專業字幕師的核心技能就是砍冗詞把資訊
塞進 CPS 上限，Netflix 明文預期譯者這樣做。而這件事**模型完全做得到** ——
它缺的只是「這句有幾秒」這個資訊，而我們手上就有。

### 設計（實際落地的版本）

```ts
// 視窗 = retime 之後真正看得到的時間（延伸到下一句開始，上限 +3 秒）
const window = clamp(min(next.start - s.start - 0.05, dur + 3), 1, ∞);
const budget = clamp(round(window * 12), 8, 32);
// 只在 budget < 24 時標註：  0: [≤14 字] I think it is a very good option
```

- **12 CPS 而不是規範的 9** —— 這是量出來的決定：全庫 2217 句的 CPS 中位數只有 4.2，
  用 9 當預算會對 **38%** 的句子下約束（其中九成本來就沒問題）。12 只碰 21%，
  而 Netflix 的 9 是「看戲」標準，我們的使用者在讀字幕學語言、還能暫停倒帶
- 視窗要**鏡射 `retimeCues`**：用語音長度算會低估預算、把譯文壓得比需要的更短
- 下限 8：太短的預算會逼出不自然的電報體
- 上限 32 = 2 行 × 16 字：再長就是 R2 的責任
- 預算 ≥24 字就不標註：模型本來就不會寫那麼長，標了只是雜訊
- **時間不可靠就不給**：video 路由的時間軸是模型估的（假的預算比沒有更糟）。
  ⚠ 原本這條寫成「只有詞級斷句才給」—— 太嚴了，**cue 邊界是 YouTube 給的真實時間**，
  照樣可用（這個錯誤讓第一輪 A/B 白跑，見 §3.1）

### 為什麼不「超標就重譯」

超標的句子重譯一次要多付一次 API —— 而它**本來就已經是可用的譯文**，只是讀起來趕。
沿用既有原則：`hints` 記下來讓它可見（`N 句超過 12 字/秒`），不自動花錢修。
真的想修，`/patch` 已經是現成的手動工具。

### 驗收 Gate

1. 先量自然變異（同設定 3 次，記 CPS > 12 的句數）—— 照 [model-reeval-sop.md](model-reeval-sop.md)
2. 加預算後 CPS > 12 的句數**顯著低於**基準分布
2.5 **確認功能真的有啟動**（`標了預算=N` 不能是 0）—— 這條是用 NT$33 買來的
3. `untranslated` 不增加；tokens 增幅 < 5%（prompt 只多幾個字元）
4. **抽樣 10 句人工比對**（不可省）：確認模型是「砍冗詞」而不是「砍語意」——
   這是本項最大的風險，自動指標測不到

### 成本／風險

半天。API 成本增加近乎零。
**風險：模型為了湊字數而丟掉資訊**。所以 gate 4 不能省，而且預算是**上限不是目標**
（prompt 要寫「≤N 字」而不是「N 字」）。

---

## 3.1 ✅ R1 實測結果（2026-08-17，prompt v6）

樣本 `hK9fypJKHyY`（英 ASR 274 句，全庫語速最密的一支：CPS 中位 8.4）。
基準與候選各 3 輪，變因只有 `CPS_BUDGET`（新增的開關，也是 production 的 kill switch）。

| 指標 | 基準（無預算） | R1（有預算） | 判讀 |
|---|---|---|---|
| **CPS>12 句數** | 31–37（mean 33.3）| **21–26（mean 23.7）** | **-29%，兩組區間完全不重疊** ✅ |
| 被標預算的句數 | 0 | 163 / 274（59%）| 這支片語速密，全庫平均約 21% |
| 預算遵守率 | — | **90%**（443/489 句次）| |
| untranslated | 0 | 0 | 沒有副作用 ✅ |
| tokens | mean 44.7k | mean 44.8k（**+0.3%**）| 遠低於 gate 的 5% ✅ |
| 成本 | NT$5.50 | NT$5.37 | 沒有變貴 |

**精準性檢查（最重要的一項）**——「該壓的才壓」還是「全面變短」？

| | 基準 | R1 |
|---|---|---|
| **被標預算**的句子 平均字數 | 13.4 | **12.1（-1.3）** |
| **沒標預算**的句子 平均字數 | 15.8 | **15.9（±0）** |

沒標的句子**完全沒動**。模型只在被下約束的地方壓縮 —— 這正是我們要的行為。

**人工抽樣（gate 4）**：讀 12 句連續的原文/譯文對照，品質可接受。
ASR 碎片的譯文逐句接得起來（「按下去就能讓」→「配對的手機響起」），沒有語意遺失。
唯一突兀的「那就太棒了」，資訊在下一句「這能讓它脫穎而出」補回來了 ——
那是碎片翻譯的固有現象，基準版一樣有。

### ⚠ 一個白跑的實驗，與它的教訓

**第一輪 A/B（NT$33）測了個寂寞**：樣本影片的 source 沒有詞級時間（`segs` 比例 0），
`Sentence` 就沒有 `start/end`，`charBudget` 於是對**每一句**回傳 undefined ——
R1 在「候選組」裡根本沒有啟動，而且**完全不會報錯**。33.3 → 29.0 的差異純粹是雜訊。

根因是我把「時間可靠」的判準訂太嚴：**cue 邊界的時間是 YouTube 給的真實時間**，
只有 video 路由的時間軸才是模型估的。修法是讓 `segmentCues` 的 cue 級路徑也帶上
start/end（精度不如詞級，但足以算顯示視窗）。

教訓（已寫進 ab-runner）：**A/B 一定要有一個「功能真的有啟動」的計量**。
runner 現在每輪都印 `標了預算=N`，`0` 就代表這輪白跑，一眼看得出來 ——
這比事後回頭猜為什麼沒效果便宜太多了。

### 還沒解決的

- 仍有 21–26 句超過 12 CPS：模型遵守率 90%，加上預算 ≥24 字的句子不標註（盲區）
- 這支片 59% 被標，是全庫最密的極端值；一般日文片約 21%
- **舊片不受影響**（R1 只作用在之後翻的片）—— 事後套用要走 R4b

## 3.2 R4b 上線後的第一次實戰（2026-08-21）

七支舊片按 📏 壓縮之前的存量，以及 `hK9fypJKHyY` 按下去之後的結果：

| 影片 | 超標句 | 佔比 | 中位顯示秒數 | 按鈕後 |
|---|---|---|---|---|
| hK9fypJKHyY（Google 發表會快剪）| 49/274 | 17.9% | 1.83s | **11**（改寫 38 句）|
| kCIvgJklMWQ | 16/512 | 3.1% | 2.55s | 未按 |
| 3weQQZ2dKCo | 6/479 | 1.3% | | 未按 |
| AzdFuFPmr6U | 3/121 | 2.5% | | 未按 |
| 9gHJwbSHPus | 1/679 | 0.1% | | 未按 |
| JM-oaFXgaSg、b14WuvF3Hn0 | 0 | | | — |

**存量落在計畫預期的 3% 尾巴**，只有 hK9fypJKHyY 是 17.9% 的離群值 ——
原因不是譯文囉嗦，是**它的 cue 本來就短**（中位 1.83s、p10 1.31s，快剪科技節目）。
時間不夠讀就是不夠讀，這種片壓縮只能救一部分。

### 剩下 11 句壓不動的原因（逐句看過）

一句都不是「譯文太囉嗦」，是兩個**別的**問題借 CPS 的殼顯形：

1. **英文夾註吃掉字數預算**（11 句裡 4 句，拿掉夾註立刻達標）
   例：`CR2032 鈕扣電池（CR2032 coin cell battery），這` —— 39 字塞進 2.35 秒，
   其中 26 字是夾註。而且**同一份資訊已經有兩個地方在講**：下面那行原文、以及
   `attachGlossaryNotes` 掛的 note 欄位。夾註是第三份。
   注意分布：全片只有 8/274 句有夾註，卻佔了超標句的 4/11 ——
   **有夾註的句子超標率 50%，全片基準 4%**（kCIvgJklMWQ 同樣現象：16 句超標裡 7 句有夾註）
2. **半句**（其餘 7 句）：`他們可以透過 Find My Device`、`這是 Google 有史以來第一次`、
   `…特別報導。請` —— 句子被切在 cue 邊界上。碎片沒有東西可以刪，模型只能原地打轉。
   這是既有的**子句邊界漂移**，壓縮救不了（R2b／未來的合併再切才是它的解）

**所以「還剩 11 句」不是壓縮失敗，是壓縮誠實地停在它管不到的地方。**
`patchStep` 對這些句子保留原譯文（改出來更長就不換），比硬塞一個更糟的說法好。

### R5：壓縮前先零成本剝夾註 —— **已實作（2026-08-21）**

`cps` 模式送 LLM 之前，先把超標句裡的 `（英文）` 拿掉、重算一次 CPS，
達標的就不必送模型。**deterministic 的修法本來就該排在花錢的修法前面。**

- 只對**超標句**做（沒超標的夾註留著，它有它的價值）
- 括號裡沒有拉丁字母的一律不動：`（笑）`、`（尋找我的裝置）` 是說明，不是重複
- 整句只有夾註就原樣保留（剝完變空字串 = 毀掉字幕）
- 模型重譯時常常把夾註加回來 —— 輸出端再剝一次，標準前後一致
- 術語解釋不會消失：note 欄位與原文行都還在

`為 Android 裝置推出原廠（First-party）追蹤器，` → `為 Android 裝置推出原廠追蹤器，`

日文片的 `柴爐（薪ストーブ）` 刻意保留 —— 括號裡沒有拉丁字母就不碰，寧可漏剝也不要剝錯。

### ✅ R5 上線後的 production 實績（2026-08-21）

| 影片 | 按 📏 之前 | 之後 | 拆解 |
|---|---|---|---|
| hK9fypJKHyY | 11 | **3** | 剝夾註 4 句（零成本）+ 重譯 5 句 |
| kCIvgJklMWQ | 16 | **0** | 剝夾註 7 句（零成本）+ 重譯 11 句 |

**兩支片當天總花費 NT$1.55**（11k tokens、4 次 LLM 呼叫）——
對照「整片重翻」的 NT$11.68 + NT$20.62，這就是事後套用路徑的意義。

比上線前的乾跑預測更好：預測是 11→7、16→11（只算剝夾註），
實際上剝完之後**剩下的句子重譯也大多成功了** —— 因為夾註消失後模型有更多字數空間可用。
hK9fypJKHyY 剩的 3 句就是 §3.2 說的半句／短 cue，壓縮確實管不到。

更上游的修法是**頻道表本身別把夾註寫進 `zh`**（`推論（Inference）` → `推論`），
人工可控、零程式；G2 儀表板要做的話順手加個檢查。

## 3.3 R1/R4b/R5 之後的重量（2026-08-21，全庫 31 支 10,080 句）

原計畫說「R1 做完再量一次，才決定 R2b/R4 要不要做」——這是那次量測。

| 指標 | 佔比 | 對應 |
|---|---|---|
| 一行放不下（>16 字）| **37.5%** | R2a 折行 |
| 兩行也放不下（>32 字）| 11.6% | R2b 拆塊，或 R3 的行數預算給到三行 |
| 三行也放不下（>48 字）| 5.5% | 只有拆塊能解 |
| 顯示 >7 秒（規範上限）| 7.3% | 拆塊 |
| **R2b 真正的目標**（>48 字 或 >7 秒）| **8.4%** | |
| CPS>12 | **3.9%** | R1+R4b+R5 已經處理過的 |

**讀法**：CPS 從當初的「尾巴 3%」問題已經被壓到 3.9% 並且有事後套用路徑；
但**行長是另一個量級的問題** —— 37.5% 不是尾巴，是三分之一。
R1 只管「時間夠不夠讀」，管不到「一行塞不塞得下」，兩者本來就正交。

### 建議（決策欄仍待人工確認）

- **R2a + R3 做**：純顯示層、零 LLM、可即時開關、涵蓋 37.5%。
  兩者共用行數預算所以必須一起做（中文折兩行 + 原文 = 畫面三行）
- **R2b + R4a 先不做**：R2a 折行後兩行能吃到 32 字，R3 的預算在「只中文」模式還能給到三行，
  也就是 11.6% 裡多數會被吸收掉；真正非拆不可的是 8.4%。
  而拆塊會動到 cue 結構、破壞 `retimeCues` 的冪等錨點 ——
  **等 R2a+R3 上線後再量一次「還剩多少真的看不下去」再決定**（這正是原計畫的順序）

### 順帶查到的：15 支 schema v1 舊片

2026-07-18 ～ 08-13 之間翻的 15 支影片還是 v1（cue 用 `en` 而非 `orig`）。
**顯示沒問題**（`player.ts` 會把 `orig` 正規化成 `en`），但它們的斷句是舊時代的產物：
`qWGudZH0cZU` 與 `h7XVJ64IhY4` 每一句都超過 32 字，樣本是 **25 秒 / 168 字一句**。
那不是排版問題，是**斷句壞掉**（docs/lessons-learned.md §4「先看 sentences 再怪模型」）——
排版救不了，要嘛重翻要嘛不管。**別把它們算進 R2 的效益裡**。

## 4. R2 — 顯示折行與拆塊 · 一天

分成三段，難度遞增：

### (a) player 折行（零風險，先做）
中文超過 16 字自動折成兩行。純 CSS/排版，不動資料。
⚠ 我們的兩行已經被「中文 + 原文」用掉了 —— 中文折兩行 = 畫面三行。
**折不折要看 R3 的行數預算**（§5）：預算夠就折，不夠就縮字級塞一行。
兩項共用同一套預算邏輯，所以 R2a 與 R3 應該一起做。

### (b) 超長句拆塊（動到 cue 結構，小心）
`36 字 / 6 秒` 這種**時間夠但一塊太長**的，拆成兩塊分次顯示（各 18 字 / 3 秒）：

- 切點：標點優先（，、；），沒有標點就找最接近中點的詞界
- 時間：按字數比例分配，兩塊都要 ≥ 最短顯示時長（`retime.ts` 的 `minDurFor`）
- **時間不夠的（36 字 / 2 秒）拆了也沒用** —— 那是 R1 的責任，這裡不碰

### (c) 冪等性（這是最容易出事的地方）
`retimeCues` 的冪等靠 `end0` 錨點，前提是**一句一 cue**。拆塊會破壞這個假設：
重跑「⏱ 修時間」會不會把已經拆過的再拆一次？

解法：拆出來的塊標記 `split: true` 並記 `srcId`，拆塊函式看到 `split` 就跳過。
**驗收必須包含「連按兩次修時間，結果完全相同」**。

### 不切在詞中間？
中文標點密度高，光靠標點就能處理絕大多數。真要詞界感知，
先試 `Intl.Segmenter`（V8 內建，**要先確認 Workers 的 ICU 有帶 word granularity**，
一分鐘可測）；不行就退回「不切在數字/英文單字中間」的簡單規則。
**不要為此引入外部斷詞服務**（見 §7）。

## 5. R3 — 前一句留著（roll-up）· 半天 · **產品決策**

使用者提的：「前一列不會馬上消失」。

- 這在業界確實存在，但定位是**直播字幕（live CC）**；預錄字幕的規範不建議
  （會佔畫面、觀眾容易分心）
- 有趣的是我們已經在 `ext/normalize.js` **主動跳過** YouTube 的 roll-up 列
  （`aAppend === 1`），因為那些列的內容是重複累積的
- **但我們的產品定位不同**：規範假設你在看戲，我們的使用者在**看外語學東西** ——
  前一句留著（淡化）是合理的閱讀輔助

### 設計：不是開關，是「行數預算」

「關掉原文才有空間放前一句」這個直覺是對的，但它其實是更一般規則的特例。
真正的限制是**畫面能給幾行**，而那隨情境變動：

| 情境 | 行數預算 | 理由 |
|---|---|---|
| 桌機／劇場模式 | 4 | 字幕在影片下方，空間充足 |
| 手機直向 | 3 | 字幕在影片下方，但螢幕窄 |
| 手機橫向（字幕疊畫面） | **2** | 疊在畫面上，多一行就是擋畫面 |

字幕區的組成：`[前一句（淡）] + [中文（可能折兩行）] + [原文]`

**超出預算時由下往上砍，順序是原則性的**：

```
1. 先砍「前一句」        ← 系統自動加的，砍掉不違背使用者意圖
2. 再砍「中文折行」      ← 改成縮字級塞一行
3. 原文永遠不砍          ← 使用者明確選了雙語模式，不能自動幫他關掉
```

**使用者明確選的東西不能被自動砍掉** —— 這條原則比任何排版規則優先。

### 設定項

`S` 面板加一個三態選項「前一句」：

| 值 | 行為 |
|---|---|
| **自動**（預設） | 行數預算有剩才顯示 —— 於是**關掉原文時它自然出現**，開雙語時自然消失 |
| 開 | 一律顯示（桌機想忍受四行的人） |
| 關 | 一律不顯示（維持現在的體驗） |

「自動」正是使用者要的行為，但用預算表達的好處是：手機橫向就算只留中文也不會
硬塞第二行，桌機開雙語也還有空間放前一句。

### 前一句的細節規則

- **間隔 > 5 秒就不顯示**：場景已經換了，留著上一句會誤導
- 只佔一行，超過就截斷加省略號（前一句是輔助，不是主角）
- 50% 透明度、小一級字
- 影片開頭沒有前一句時不留空白（不是塞空行，是不渲染）

### 加分項（可選，成本極低）

**點前一句 = 跳回去重播那句**。player 已經有 seek，這只是加個 click handler。
對「剛才那句沒聽清楚」的學習場景很有用 —— 而這正是本專案存在的理由。

### 驗收
- **四種字幕模式 × 三種情境**的實際行數都不超過預算
- 手機橫向最大化時不遮擋畫面
- 切換字幕模式時不閃爍、不跳版
- 影片開頭沒有前一句時不留空白

## 6. R4 — 對已翻好的影片套用（`/admin` 按鈕）· 半天

R1–R3 只影響「之後翻的片」，但**手上的片都已經翻好了**。
為了 3% 的句子整片重翻（`?force=1`）等於付 100% 的錢修 3% 的問題 —— 不合理。

### 這其實是本專案已經成形的模式

| 按鈕 | 機制 | 成本 |
|---|---|---|
| ⏱ 修時間 | deterministic 重算顯示時間軸（`retimeCues`）| 零 |
| ✏ 補譯 | 只重譯未譯／原文照抄的句子（`patchStep`）| 以句計價 |
| **✂ 修排版**（未做） | deterministic 拆長句 + 重算時間 | **零** |
| **📏 壓縮過長句** ✅ | 先剝夾註（零成本）→ 剩下的才重譯 | 以句計價（約 3%，其中一半被 R5 免費解掉）|

**原則：每個品質改善都要有「事後套用」的路徑。**
語料已經翻好了，重跑整片是最貴也最笨的修法 —— 這條原則值得寫進 CLAUDE.md。

### R4a「✂ 修排版」= R2b 的事後版（零 LLM）

跟 ⏱ 修時間同一類：純函式、deterministic、冪等。
把 R2b 的拆塊邏輯抽成 `resplitCues(cues)`，`/admin` 加按鈕、`POST /resplit/{id}`。

- 拆完接著跑 `retimeCues`（兩者本來就該連著跑）
- 冪等靠 R2b 設計的 `split` 標記（見 §4c）——
  **驗收就是「連按三次結果完全相同」**
- 舊片沒有 `end0` 也沒關係，`retimeCues` 會自動補

### R4b「📏 壓縮過長句」= 把 `/patch` 一般化 —— **已實作（2026-08-21）**

不必寫新步驟 —— **`patchStep` 只差一個目標偵測器**：

```ts
// 現在：needsRetranslate(orig, zh, untranslated)   → 未譯／原文照抄
// 加上：exceedsCps(zh, start, end)                 → 字數 / 秒數 > 12（countCpsOver 已經寫好）
POST /patch/{id}?mode=untranslated|cps|all
```

其餘全部沿用：帶前後語境的小請求、上限 2 輪、warnings 重寫、四層保險絲。
重譯時把 R1 的字數預算一起丟進 prompt（同一段程式）。

- 成本：一支 500 句的片約 15 句超標 → 一次呼叫、NT$0.2 上下
- **模式預設是 `untranslated`**（維持現有行為）；`cps` 要明示 ——
  自動壓縮會改動使用者已經在看的譯文，那必須是人為決定
- 壓縮後仍超標的不再重試（同 §3「不自動花錢修」的原則），記 hint

實作後補上的三件事（原計畫沒寫，但缺了會出事）：

1. **字數上限用「實際顯示時間」算**（`cueBudget`），不是翻譯當下的預估 ——
   retime 早就跑過了，bilingual 的 `start/end` 比 sentences 的估計準
2. **改出來更長就不換**：模型壓不動時常見的失敗是換句更長的說法。
   保留原譯文比接受一個更糟的結果好（「寧可看得見地失敗」的同一條原則）
3. **`/jobs.json` 回填 `cpsOver`**：舊片的 status.json 沒有這個欄位，
   不回填的話儀表板永遠是 0，按鈕永遠不出現 —— 事後套用路徑等於沒有
   （同 P2 對 `untranslated` 做過的事）

### 驗收
- ✂ 修排版（未做）：連按三次結果相同；拆出來的塊都 ≤2 行；SRT 同步更新
- 📏 壓縮 ✅ 已驗：只有超標句被改動、未譯數不增加、時間軸不動、SRT 同步更新、
  沒有目標時零 API 呼叫、剝完達標就不送模型、被 40 句上限切掉會接著補完
  （`worker/test/integration.test.ts` 的「壓縮補譯 mode=cps」7 個案例）
- 兩顆按鈕都只在 `route === 'text'` 顯示（video 路由時間軸是模型估的，見 §3）
- **人工抽樣仍不可省**：R4b 的 38 句改寫沒有做逐句人工比對（只看了剩下的 11 句），
  下次按 📏 時值得抽 10 句看一眼

## 7. 明確不做

- ❌ **外部斷詞服務（CKIP-transformers 等）**：它是繁中斷詞/POS/NER，
  而我們的**斷句發生在原文側（英/日 ASR）**，用不上；就算用在中文切分，
  PyTorch + 幾百 MB 模型跑不進 Workers，得另養服務 —— 與自用規模不成比例
- ❌ **讓模型決定 cue 邊界**：它不知道時間軸，也看不到整片（分塊平行翻譯）
- ❌ **超標句自動重譯**：貴，而且原譯文本來就可用（見 §3）
- ❌ **自動改寫已翻好的舊片**：R1–R3 只影響之後翻的片。舊片**手動**套用走 R4 的兩顆按鈕
  （§6）—— 自動去改使用者已經在看的譯文，是不請自來的驚喜，不是功能
- ❌ **用 `?force=1` 修排版問題**：整片重翻 = 為 3% 的句子付 100% 的錢

## 8. 決策（人工填寫）

```
R1 壓縮譯文（prompt 字數預算）：**已做**（2026-08-17）—— CPS>12 減 29%、沒有副作用、
   沒標的句子完全不動。CPS_BUDGET=off 可隨時關掉
R2a player 折行：____（做 / 不做）—— 建議做（37.5% 的句子一行放不下，§3.3）
R2b 超長句拆塊：____（做 / 不做 / 等 R1 之後再看還剩多少）
   —— 建議等 R2a+R3 上線後再量；真正非拆不可的是 8.4%，而拆塊會動 cue 結構（§3.3）
R3 前一句留著：____（行數預算 + 三態設定 / 只做簡單開關 / 不做）
   └ 點前一句跳回重播：____（一起做 / 之後再說）
R4a ✂ 修排版按鈕（零 LLM）：____（做 / 不做）—— 綁 R2b，R2b 不做就不用做
R4b 📏 壓縮過長句按鈕（/patch?mode=cps）：**已做**（2026-08-21）—— 舊片也修得掉，
   免得 R1 只對「之後的新片」有效
R5 壓縮前零成本剝英文夾註：**已做**（2026-08-21）—— production 實績：hK9fypJKHyY 11→3、
   kCIvgJklMWQ 16→0，兩支合計 NT$1.55。依據見 §3.2
日期：2026-08-21
理由：R1 上線後手上七支片仍有讀不完的句子（R1 只作用在之後翻的片）。
   實作如 §6 預期：patchStep 只多一個目標偵測器，沒有新步驟
```

> 建議順序：**R1 → R2a+R3（共用行數預算）→ 重新量一次 → 再決定 R2b/R4**。
> R1 是唯一治本的（時間不夠讀就是不夠讀，排版救不了）；R2a+R3 共用預算邏輯要一起做；
> R2b 動到 cue 結構，等 R1 做完再看還剩幾句需要拆，很可能不用做 ——
> **而 R4 的工作量取決於 R2b 有沒有做**（R4a 是 R2b 的事後版，R4b 只是 `/patch` 多一個偵測器）。
